# 真实凭据，已被 .gitignore 忽略，不会进仓库。
# 所有节点 SSH 用户名/密码均为 p4

SSH_DEFAULT_USERNAME=p4
SSH_DEFAULT_PASSWORD=p4

SSH_CONNECT_TIMEOUT=10
SSH_COMMAND_TIMEOUT=600

# 默认 SQLite，无需改。切 MySQL 时取消下行注释并填写：
# DATABASE_URL=mysql+pymysql://user:pass@10.130.131.239:3306/control_plane
